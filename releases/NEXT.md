# Next Release

## 0.1.0

- Add the `ownwork` CLI and `own-work` package.
- Add Task, Roadmap, Spec, and System registry workflows on the shared workspace envelope.
- Add current and legacy envelope interoperability, explicit migration, CI, smoke, and package gates.
